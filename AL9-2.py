

# 2.Crie um código em Python para exibir a contagem de dígitos de um número inteiro positivo
#  informado pelo usuário. 

def contador(number):
    contando=len(number)
    return contando

number=str(input("Informe um numero : \n"))
print(contador(number))
